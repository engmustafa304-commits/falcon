{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "corepack enable && corepack pnpm install --frozen-lockfile --prod=false && corepack pnpm --filter @falcon/backend run build:railway"
  },
  "deploy": {
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "startCommand": "corepack pnpm --filter @falcon/backend run start:railway"
  }
}
