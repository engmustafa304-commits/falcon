# seo

Reusable document metadata helpers for public Falcon pages live here.

The `Seo` component owns route-level title, description, canonical, OpenGraph,
Twitter card, and JSON-LD metadata without introducing a head-management
dependency.
